server: 
  port: 4444
  context-path: /oracle

spring:
  cloud:
    stream:
      bindings:
        sannyas: 
          group: oracle

security:
  oauth2:
    resource:
      userInfoUri: http://auth-service:5000/auth/user

logging: 
  level.guru.bonacci.oogway.oracle: INFO